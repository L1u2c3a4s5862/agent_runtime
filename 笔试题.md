## 架构设计题

### 模块一：Context / Performance

**Q1**：大模型面对第一轮长窗口或多模态输入时，first token会显著变慢。有什么快速/低成本/用户体验也不差的方案？从5-10秒稳定压缩到2秒。
**A**：我们可以用Prompt压缩或摘要路由来实现。长文本不要直接塞给大模型，我们可以先用一个极小模型做一遍压缩，提取长文本的结构化片段，大模型只处理摘要；如果是多模态的场景，我们可以先将图片用轻量VLM抽出文字描述和关键信息，把原图替换为文本描述。这样首token直接腰斩。

**Q2**：一个session连续聊了200轮，context快爆了。你会怎么做压缩？如何确保压缩后的对话仍然流畅？
**A**：首先我们需要在上下文窗口阈值在70%时启动后台静默压缩，不要让用户等到快爆的时候再压缩；在90%时启动强制压缩，只保存最近20-30轮对话。我们可以将这200轮对话分成3个时间区，早期、中期和近期。早期区处理1-50轮对话的全局结构化摘要；中期区处理51-150轮对话的分段结构化摘要（每20轮一次结构化摘要）；近期区保留151-200轮对话的全部信息，不做摘要。

### 模块二：Memory

**Q1**：和聊天Agent熟悉半个月后，用户问了一个以前问过的问题。Agent如何做memory召回更合理？
**A**：首先半个月的对话已经积累了大量内容，必须要提前做结构化存储，否则无法做召回。每次对话后，异步抽取并更新这些记忆如职业、兴趣、性格、沟通偏好、禁忌等信息。并不是每次都需要全量召回，比如在明显指代（如“你之前说的...”）时触发召回，我们可以在入口处加一个轻量分类器或规则，判断是否需要找回。在生成阶段，我们可以将找回的记忆注入到prompt中并指导模型使用找回的记忆回答当前问题。

**Q2**：你理解的Agent memory经典框架是什么？它的发展趋势是什么，最头部的玩家在怎么做？
**A**：Agent memory框架有LangChain、MemGPT（现在叫Letta）、Generative Agents。Agent memory的发展趋势有4个明确方向，1. 从存储到进化：早期的记忆只支持append-only日志，现在支持完整的增删改查（CRUD）；2. 从同步到异步：用户聊完走了，Agent在后台还在整理记忆，下次见面时，记忆已经整理好了，不用现算；3. 从文本到多模态；4. 从单Agent到多Agent共享。头部玩家如OpenAI（跨会话记忆+Agents SDK）、Anthropic（长上下文+Constitutional AI）、Google（超大上下文+多模态记忆）

### 模块三：Task

**Q1**：对于长程任务，大模型执行一段时间可能会忘掉目标，你知道哪些解决方案，有什么优缺？
**A**：首先，在长程任务中，大模型会忘掉目标本质上是指上下文窗口有限，注意力在长序列中衰退。我们可以使用无爱不记忆和RAG检索增强，将任务目标、关键信息、已完成步骤等存入外部存储（向量数据库、键值存储、文件系统），在每一步或每隔几步检索最相关的记忆注入上下文。优点是灵活扩展、检索精准、可跨会话持久化，缺点是检索耗时和开发复杂、检索时可能会漏掉关键信息。

**Q2**：用户给Agent下达任务：每天早上9点根据昨天聊天情况做复盘总结。你会怎么设计？
**A**：首先，“每天早上9点根据昨天聊天情况做复盘总结”可以分为两个部分，1. Agent在夜间后台（“睡眠”）批量处理一天的会话并生成结构化记忆；2. 在第二天早上9点生成重点摘要推送给用户。在夜间处理会话时，可以删掉寒暄、试错过程、重复确认（“好的”、“明白了”），保留代码块、决策结论、用户明确说"记住这个"的内容。

### 模块四：Tool / Session Runtime

**Q1**：Agent工具有同步和异步两类。异步工具不能让用户一直等，但结果依然重要。你会如何设计异步工具执行和完成通知？
**A**：设计异步工具可以使用状态机+事件驱动+渐进式反馈来实现，每个状态都要持久化（写到数据库），因为Agent进程可能重启，不能靠内存里的变量跟踪。执行层可以使用SSE长连接推送，优点是用户体验较好，有实时反馈，但连接需要维持，移动端弱网环境可能断开。

**Q2**：如果session state为busy，此时用户又发来新消息，或者异步工具完成事件也到达，runtime应该如何处理？
**A**：busy不能是一个粗粒度布尔值，runtime必须知道大模型在忙什么，可不可以被打断，我们可以用细粒度状态机+事件队列+可中断点来实现。事件优先级分为用户新消息→一步工具完成/系统事件（如果超时）→工具进度更新；可中断点有Prompt组装中、大模型推理中、工具参数解析中、工具执行中。

### 模块五：Agent Runtime 架构对比

**Q1**：Claude Code的工具输出方式和国内GLM/豆包等OpenAI-compatible function calling有什么不同？他们各自这样设计的优缺点是什么？
**A**：Anthropic的工具调用是使用tool_use而兼容OpenAI的国产大模型是使用tool_calls，一个工具是模型推理的一部分，对话感更自然，模型可以在一句话中间插入工具调用，而兼容OpenAI的国内大模型是一句话结束后才调用工具。但参数错误要到执行层才发现，调试成本高，和OpenAI生态不兼容，必须写适配层，而兼容OpenAI的国产大模型只需要改base_url和model名，切换成本极低，开发友好，JSON Schema工具链成熟。

**Q2**：Openhands的状态机设计有什么优缺？更优雅的实现方式是怎么样的？
**A**：OpenHands的状态机采用有限状态机管理Agent的生命周期，优点是状态少而明确，开发者容易理解Agent当前处于何种阶段，便于调试和监控；有完整事件日志，出了问题可以逐帧回溯，缺点是状态粒度太粗，状态定义模糊。
